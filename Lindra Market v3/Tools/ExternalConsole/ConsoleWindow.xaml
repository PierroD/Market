<ui:FluentWindow x:Class="Lindra_Market_v3.Tools.ExternalConsole.ConsoleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:Lindra_Market_v3.Tools.ExternalConsole"
        xmlns:views="clr-namespace:Lindra_Market_v3.Tools.ExternalConsole.Views"
        mc:Ignorable="d"
        Title="{DynamicResource Console_Title}"
        Height="650"
        Width="950"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        Icon="pack://application:,,,/Assets/app.png">
    <Grid Background="{DynamicResource ApplicationBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{DynamicResource Console_Title}">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/app.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <ui:NavigationView
            x:Name="RootNavigation"
            Grid.Row="1"
            PaneDisplayMode="LeftMinimal"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleVisible="False"
            IsPaneOpen="False">

            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem
                    Content="{DynamicResource Console_Nav_Console}"
                    Icon="{ui:SymbolIcon WindowDevTools24}"
                    TargetPageType="{x:Type views:ConsolePage}" />

                <ui:NavigationViewItem
                    Content="{DynamicResource Console_Nav_Dvars}"
                    Icon="{ui:SymbolIcon Search24}"
                    TargetPageType="{x:Type views:DvarSearchPage}" />
            </ui:NavigationView.MenuItems>
        </ui:NavigationView>

        <!-- Status bar at bottom -->
        <Border Grid.Row="2"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0"
                Padding="16,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse x:Name="StatusIndicatorDot"
                             Width="8" Height="8"
                             Margin="0,0,8,0"
                             Fill="{DynamicResource TextFillColorTertiaryBrush}" />
                    <TextBlock x:Name="StatusGameText"
                               Text="{DynamicResource Console_Status_Waiting}"
                               FontSize="12"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </StackPanel>

                <TextBlock Grid.Column="1"
                           x:Name="StatusPidText"
                           FontSize="11"
                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                           VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
